import { render, screen } from '@testing-library/react';
import { TestRecoil }  from '.';


describe('<TestRecoil />', ()=>{
  it('should render TestRecoil correctly', () => {
    render(<TestRecoil />);
  }
  )
})
